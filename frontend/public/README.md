# Public Folder

This folder is intentionally included for public static assets used by the React frontend.
The current app uses remote stadium photo URLs entered by stadium owners, so no default image files are required.
